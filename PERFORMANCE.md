# PERFORMANCE — Vaerion measured evidence

| | |
|---|---|
| **Status** | GENERATED by `packages/vaerion/scripts/perf-check.ts` — never hand-edited |
| **Environment** | bun 1.3.14 · linux x64 |
| **Verdict** | 3 budget breach(es) — recorded honestly below |

## Budgets (blueprint §3.2) vs measured

| ID | Budget | Measured | Verdict |
|---|---|---|---|
| OBJ-Q1 artifact size | ≤ 25 MB | 0.18 MB (@vaerion/engine tarball) | PASS |
| OBJ-Q2 cold start | ≤ 60 ms p50 | 81.5 ms p50 · 127.8 ms p95 (n=15, `vae version` full process wall time) | FAIL |
| OBJ-Q3 hot-path overhead | < 8 ms p50 / < 25 ms p99 | 113.0 ms p50 · 141.6 ms p99 (n=12, full `run model` incl. process start + journal fsync) | FAIL |
| OBJ-Q4 memory | ≤ 120 MB active / < 45 MB idle daemon | idle 56.7 MB max RSS (n=10 samples); active not sampled this run | FAIL |
| OBJ-Q5 index throughput | ≥ 12,000 files/min | 113,008 files/min (240 synthetic files, cold process) | PASS |
| OBJ-Q6 coverage gates | floors in bunfig.toml | enforced by the verify gate (see VERIFICATION_REPORT.md) | PASS |
| OBJ-Q7 zero secrets | asserted in fuzz tests | constitutional C5 + golden redaction + distribution secret scan | PASS |

## Honest notes


### Breaches (root-cause notes, never excuses)

- OBJ-Q2 cold-start p50 81.5ms > 60ms budget
- OBJ-Q3 hot-path p50 113.0ms / p99 141.6ms vs <8ms/<25ms budget (measured end-to-end incl. process start + per-record fsync journal writes)
- OBJ-Q4 idle daemon RSS 56.7MB >= 45MB budget

The OBJ-Q2/OBJ-Q3/OBJ-Q5 budgets were authored for the compiled per-platform
substrate the blueprint targeted. The reference substrate is TypeScript-on-Bun
(ADR-0018, Proposed — pending Founder ratification): process start and
per-record fsync journal writes dominate the measured path. These numbers are
recorded as evidence for the substrate decision, not as excuses — a ratified
substrate change re-baselines the budgets; nothing here weakens a gate.

Reproduce: `bun run packages/vaerion/scripts/perf-check.ts` (exit 1 on any breach).
